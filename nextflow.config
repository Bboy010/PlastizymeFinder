/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    PlastizymeFinder Nextflow config file
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
*/


params {

    // -----------------------------------------------------------------------
    // INPUT / OUTPUT
    // -----------------------------------------------------------------------
    input           = null          // Path to input samplesheet (CSV)
    outdir          = 'results'     // Output directory
    tracedir        = "${params.outdir}/pipeline_info"

    // -----------------------------------------------------------------------
    // REFERENCE
    // -----------------------------------------------------------------------
    host_genome     = null          // Path to host genome (FASTA) for decontamination

    // PETase reference structure for TM-Align (Stage 8)
    // Default: 6EQE (IsPETase, Ideonella sakaiensis) — auto-downloaded from RCSB if null
    // Provide your own with: --petase_ref /path/to/custom.pdb
    petase_ref      = null

    // -----------------------------------------------------------------------
    // DATABASES
    // -----------------------------------------------------------------------
    // Provide your own paths, or leave null to auto-download at runtime

    kraken2_db      = null          // Kraken2 database directory
    metaphlan4_db   = null          // MetaPhlAn4 database directory
    dbcan2_db       = null          // dbCAN2 database directory
    eggnog_db       = null          // eggNOG database directory
    kofamscan_db    = null          // KofamScan profiles directory
    gtdbtk_db       = null          // GTDB-Tk reference data directory
    checkm_db       = null          // CheckM database (set with: checkm data setRoot /path)

    // NCBI CDD profiles for local RPS-BLAST (Stage 8) — offline Batch CD-Search
    // Leave null to auto-download. cdd_db_set selects which pre-formatted set:
    //   'Cdd'  ~1.7 GB — full CDD, 62,456 models (production)
    //   'Pfam' ~410 MB — Pfam subset only (CI and test runs)
    cdd_db          = null
    cdd_db_set      = 'Cdd'

    // PET_DB : required — no auto-download (manual curation required)
    // Guide: docs/pet_db_guide.md
    pet_db          = null

    // Directory where databases will be downloaded if not provided
    db_cache_dir    = "${projectDir}/databases"

    // -----------------------------------------------------------------------
    // PIPELINE OPTIONS
    // -----------------------------------------------------------------------
    skip_taxonomy       = false     // Skip taxonomic profiling (Stage 2)
    skip_annotation     = false     // Skip gene annotation (Stage 6)
    skip_structure      = false     // Skip 3D structure prediction (Stage 8)
    skip_plastizyme     = false     // Skip METATARENZ plastizyme prediction (Stage 7)
    skip_drep_checkm    = false     // Skip CheckM quality check in dRep (requires CheckM installed)

    // Assembly
    assembler       = 'megahit'     // De novo assembler (megahit only for now)
    min_contig_len  = 1500          // Minimum contig length — MetaBAT2 hard minimum is 1500 bp

    // Binning
    min_bin_size    = 200000        // Minimum bin size (bp) for MetaBAT2

    // Plastizyme prediction (Stage 7)
    // MeTarEnz screening mode: 'cs' screens nucleotide contigs with BLASTX,
    // 'ps' screens pre-translated proteins with BLASTP.
    metarenz_mode     = 'cs'
    metarenz_bitscore = 50          // Minimum bit-score for a candidate to be reported

    // Bin QC thresholds (dRep)
    min_completeness    = 50        // Minimum bin completeness (%)
    max_contamination   = 10        // Maximum bin contamination (%)

    // -----------------------------------------------------------------------
    // BOILERPLATE OPTIONS
    // -----------------------------------------------------------------------
    help            = false
    version         = false
    publish_dir_mode = 'copy'
    email           = null
    email_on_fail   = null
    multiqc_config  = null
    multiqc_title   = null
    max_multiqc_email_size = '25.MB'
    monochrome_logs = false

    // -----------------------------------------------------------------------
    // MAX RESOURCE LIMITS (overridden by profiles)
    // -----------------------------------------------------------------------
    max_cpus        = 16
    max_memory      = '128.GB'
    max_time        = '240.h'
}

// -----------------------------------------------------------------------
// PROCESS DEFAULTS
// -----------------------------------------------------------------------
process {
    resourceLimits = [
        cpus:   params.max_cpus as int,
        memory: params.max_memory,
        time:   params.max_time
    ]
    cpus   = { 2    * task.attempt }
    memory = { 8.GB * task.attempt }
    time   = { 4.h  * task.attempt }

    errorStrategy = { task.exitStatus in [143,137,104,134,139] ? 'retry' : 'finish' }
    maxRetries    = 2
    maxErrors     = '-1'
}

// -----------------------------------------------------------------------
// INCLUDE CONFIGS  (loaded before profiles so profiles can override them)
// -----------------------------------------------------------------------
includeConfig 'conf/base.config'
includeConfig 'conf/modules.config'

// -----------------------------------------------------------------------
// PROFILES
// -----------------------------------------------------------------------
profiles {

    debug {
        process.beforeScript = 'echo $HOSTNAME'
    }

    docker {
        docker.enabled         = true
        docker.runOptions      = '-u $(id -u):$(id -g)'
        singularity.enabled    = false
        conda.enabled          = false
    }

    singularity {
        singularity.enabled     = true
        singularity.autoMounts  = true
        singularity.pullTimeout = '60 min'
        docker.enabled          = false
        conda.enabled           = false
    }

    conda {
        conda.enabled          = true
        docker.enabled         = false
        singularity.enabled    = false
    }

    gpu {
        // Enables the accelerator directive on every 'process_gpu' process
        // (see conf/base.config) and exposes the host GPUs to the container.
        docker.runOptions      = '-u $(id -u):$(id -g) --gpus all'
        singularity.runOptions = '--nv'
        apptainer.runOptions   = '--nv'
    }

    test {
        includeConfig 'conf/test.config'
    }

    // Comme `test`, mais sans sauter l'étape 8. Un profil plutôt qu'un
    // `--skip_structure false` en ligne de commande : depuis Nextflow 25, un
    // paramètre passé ainsi arrive comme la chaîne "false", qui est vraie.
    test_all {
        includeConfig 'conf/test.config'
        params.skip_structure = false
    }

}

// -----------------------------------------------------------------------
// EXECUTION REPORTS
// -----------------------------------------------------------------------
timeline {
    enabled   = true
    overwrite = true
    file      = "${params.tracedir}/execution_timeline.html"
}
report {
    enabled   = true
    overwrite = true
    file      = "${params.tracedir}/execution_report.html"
}
trace {
    enabled   = true
    overwrite = true
    file      = "${params.tracedir}/execution_trace.txt"
}
dag {
    enabled   = true
    overwrite = true
    file      = "${params.tracedir}/pipeline_dag.html"
}

// -----------------------------------------------------------------------
// MANIFEST
// -----------------------------------------------------------------------
manifest {
    name            = 'PlastizymeFinder'
    author          = 'Hongo'
    homePage        = 'https://github.com/Bboy010/PlastizymeFinder'
    description     = 'Metagenomic pipeline to identify and characterize plastic-degrading enzymes'
    mainScript      = 'main.nf'
    nextflowVersion = '!>=24.10.0'
    version         = '1.0.0'
}

